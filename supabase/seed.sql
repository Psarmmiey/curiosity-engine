-- Development seed data
-- Only run this in local development, never in production

-- Insert a test user (requires a matching auth.users entry)
-- Run AFTER creating a user via Supabase Auth in local dev

-- Sample categories reference
-- The app uses these exact string values:
-- 'Science', 'Nature', 'History', 'Psychology', 'Technology',
-- 'Art & Culture', 'Economics', 'Philosophy', 'Surprise Me'
